const config = {
  accessKey: String(import.meta.env.VITE_PEXEL_ACCESS_KEY),
  secretKey: String(import.meta.env.VITE_UNSPLASH_SECRET_KEY),
  appIdKey: String(import.meta.env.VITE_APP_ID_KEY),
};

export default config;
